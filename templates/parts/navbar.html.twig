
    <nav class="navbar navbar-expand-lg navbar-light bg-primary py-3">
        <div class="container-fluid">
            <a class="navbar-brand text-light fs-4 fw-bold" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">

                    <li class="nav-item">
                        <a class="nav-link text-light fs-5 px-3" aria-current="page" href="{{ path ('app_nft') }}">All NFT</a>
                    </li>

                    {% if app.user is not null %}
                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_nft_user') }}">My NFT</a>
                        </li>
                    {% endif %}

                    {# ne pas oublier d'ajouter "if app.user is not null and ..." car
                la méthode appartient à l'utilisateur #}
                    {% if app.user is not null and app.user.hasRole("ROLE_ADMIN")%}
                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_admin') }}">Administration</a>
                        </li>
                    {% endif %}

                    {% if app.user is null %}
                        {#si aucun user n'est connecté#}
                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_login') }}">Login</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_register') }}">Register</a>
                        </li>
                    {% endif %}

                    {% if app.user is not null %}
                        {#si un user est connecté#}
                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_logout') }}">Logout</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_account') }}">Update account</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-light fs-5 px-3" href="{{ path ('app_nft_form') }}">Add a NFT</a>
                        </li>


                    {% endif %}

                </ul>
            </div>
        </div>
    </nav>
